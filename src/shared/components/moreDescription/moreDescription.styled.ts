import styled from "styled-components";

const CollapseText = styled.span`
  color: #248bc0;
  cursor: pointer;
  font-weight: 600;
  padding: 8px 0 8px 0;
`;

const ReducedDescriptionDiv = styled.div`
  height: 20px;
  overflow-y: hidden;
`;

export { CollapseText, ReducedDescriptionDiv };
