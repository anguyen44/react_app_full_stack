import { Server } from "icons";
import styled from "styled-components";

export const PortfolioTeamLogo = styled(Server)`
  height: 2em;
  width: 2em;
  color: ${({ theme }) => theme.palette.enedis.secondary.blue["500"]};
`;
