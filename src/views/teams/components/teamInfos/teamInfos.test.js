import { render } from "test/utils";

import TeamInfosComponent from "./TeamInfos.component";

describe("tests teamInfos component", () => {
  test("test render component", () => {
    render(<TeamInfosComponent />);

    // TODO : check styled
  });
});
