import styled from "styled-components";

const CustomCardTitleInPage = styled.span`
  font-size: 15px;
  color: ${(props) => props.theme.palette.enedis.grey["800"]};
  padding-left: 5px;
  font-weight: 600;
`;

export { CustomCardTitleInPage };
